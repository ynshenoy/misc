ADMIN_TOKEN=<%=@admin_token%>
AUTH_URL=<%=@auth_url%>
ADMIN_DOMAIN_NAME=<%=@admin_domain_name%>

ADMIN_DOMAIN=$(curl -H "X-Auth-Token: $ADMIN_TOKEN" $AUTH_URL/domains?name=$ADMIN_DOMAIN_NAME | jq .domains[0].id | tr -d '"')

if [ $ADMIN_DOMAIN = 'null' ]; then
    exit 1
else
    exit 0
fi
